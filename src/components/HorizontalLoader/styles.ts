import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  progressBarWrapper: {
    alignItems: 'center',
    justifyContent: 'center',
    marginHorizontal: 8,
    marginBottom: 8,
  },
  progressBarHide: {
    opacity: 0,
  },
  progressBarShow: {
    opacity: 1,
  },
});
